spring:
  jpa:
    database: POSTGRESQL
    show-sql: false
  database:
    driverClassName: org.postgresql.Driver
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/ultracarweb_user_test?currentSchema=public
    username: postgres
    password: postgres
